// priority: 0
"use strict";

function registerTotemicLoots(event) {

    /*
    event.getLootTable('totemic:entities/buffalo')
        .modifyItemEntry(entry => {
            if (entry.item.id === 'totemic:buffalo_hide') {
                entry.item = Item.of('tfc:large_raw_hide')
                entry.setCount(1)
            }
            return entry
        })
            */
}